# Python deps for the benchmark harness (producer, metrics, analysis, charts).
# Install into a venv:  python -m venv .venv && .venv/bin/pip install -r bench/requirements.txt
confluent-kafka==2.15.0   # high-throughput Kafka producer (common/producer.py)
pyiceberg==0.10.0         # drop table between runs (local REST catalog)
pyarrow==21.0.0           # pyiceberg scan/count backend
requests==2.32.5          # REST catalog + Flink REST metrics
matplotlib==3.9.4         # bench/make_charts.py
tabulate==0.9.0           # comparison tables
